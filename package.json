{
  "name": "perfect",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test --recursive packages/",
    "test:core": "cd packages/core && bun test",
    "test:transform": "cd packages/transform && bun test",
    "typecheck": "bun run typecheck:core && bun run typecheck:packages",
    "typecheck:core": "cd packages/core && bun run typecheck && bun run typecheck:types",
    "typecheck:packages": "cd packages/http && bun run typecheck && cd ../http-otel && bun run typecheck && cd ../transform && bun run typecheck",
    "build:packages": "cd packages/core && bun run build && cd ../http && bun run build && cd ../http-otel && bun run build && cd ../transform && bun run build",
    "smoke:packages": "bun scripts/smoke-package-exports.ts",
    "lint": "oxlint -c .oxlintrc.json packages/",
    "fmt": "oxfmt packages/ '!**/dist/**'",
    "fmt:check": "oxfmt --check packages/ '!**/dist/**'",
    "format": "bun run fmt",
    "bench": "cd packages/core && bun run bench/index.ts",
    "bench:vs-effect": "cd packages/core && bun run bench/vs-effect-ts.ts",
    "perf:report": "cd packages/core && bun run bench/perf-report.ts",
    "perf:gate": "cd packages/core && bun run bench/perf-gate.ts",
    "documentation:build": "bun documentation/build.ts",
    "documentation:check": "bun documentation/build.ts --check",
    "documentation:dev": "bun documentation/build.ts && vitepress dev documentation",
    "documentation:build:html": "bun documentation/build.ts && vitepress build documentation",
    "documentation:preview": "vitepress preview documentation",
    "build:rust": "cd crates/perfect-transform && cargo build --locked --release",
    "build:swc": "rustup target add wasm32-wasip1 && cd crates/swc-plugin-perfect && cargo build --locked --target wasm32-wasip1 --release && mkdir -p ../../packages/swc-plugin/dist && cp target/wasm32-wasip1/release/swc_plugin_perfect.wasm ../../packages/swc-plugin/dist/plugin.wasm"
  },
  "devDependencies": {
    "@nx/js": "^22.6.5",
    "@types/bun": "latest",
    "@types/markdown-it-container": "^4.0.0",
    "markdown-it-container": "^4.0.0",
    "nx": "^22.6.5",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0",
    "rxjs": "^7.8.2",
    "typescript": "^6.0.2",
    "vitepress": "^1.6.4"
  }
}
