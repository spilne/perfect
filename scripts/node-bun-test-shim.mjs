import * as nodeTest from 'node:test';
import { expect } from 'expect';

export const {
  describe,
  it,
  test,
  before,
  beforeEach,
  beforeAll,
  after,
  afterEach,
  afterAll,
} = nodeTest;

export { expect };

export const setDefaultTimeout = () => {};

